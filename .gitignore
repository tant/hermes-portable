# Ignore runtime cache and user data
data/
.cache/
src/
*.flag
*.log

# Live test sandbox (real runtime downloads, never committed)
test-run/

# Internal planning docs (kept local, not published)
docs/superpowers/
